# Contributing

## Folders
Files of specific types myst go in their respective repositories as listed below:
* Pictures in `images/`
* Stylesheets in `css/`
* Javascript in `js/`

Additionally, feature snippets must be stored in their subdirectories. E.g, slideshow images must be stored in `images/slideshow`
